apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attachment-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: init-database-attachment-configmap
data:
  init-database-attachment.sql: |
    CREATE DATABASE "thesis_management_attachments" 

---

#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-attachment-db-service
spec:
  selector:
    app: thesis-management-backend-attachment-db 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432


---
#For creating Kubernetes Deployment in the Minikube cluster
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thesis-management-backend-attachment-db
  labels:
    app: thesis-management-backend-attachment-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-attachment-db
  template:
    metadata:
      labels:
        app: thesis-management-backend-attachment-db 
    spec:
      containers:
      - name: thesis-management-backend-attachment-db
        image: qthuy2k1/thesis-management-backend-attachment-db
        env:
        - name: POSTGRES_PASSWORD
          value: "root"
        imagePullPolicy: Always
        ports:
        - containerPort: 5432
        volumeMounts:
          - name: attachment-db
            mountPath: /var/lib/postgresql/data
          - name: init-database-script
            mountPath: /docker-entrypoint-initdb.d/init-database-attachment.sql
            subPath: init-database-attachment.sql
      volumes:
        - name: attachment-db
          persistentVolumeClaim:
            claimName: attachment-db-pvc
        - name: init-database-script
          configMap:
            name: init-database-attachment-configmap
      