#For creating Kubernetes Deployment in the Minikube cluster
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thesis-management-backend-post
  labels:
    app: thesis-management-backend-post
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-post
  template:
    metadata:
      labels:
        app: thesis-management-backend-post 
    spec:
      containers:
      - name: thesis-management-backend-post
        image: qthuy2k1/thesis-management-backend-post
        imagePullPolicy: Always
        ports:
        - containerPort: 9091
      
        

---
#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-post-service
spec:
  selector:
    app: thesis-management-backend-post 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 9091
    targetPort: 9091