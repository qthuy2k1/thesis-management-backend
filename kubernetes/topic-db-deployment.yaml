apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: topic-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: init-database-topic-configmap
data:
  init-database-topic.sql: |
    CREATE DATABASE "thesis_management_topics" 

---
#For creating Kubernetes Deployment in the Minikube cluster
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thesis-management-backend-topic-db
  labels:
    app: thesis-management-backend-topic-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-topic-db
  template:
    metadata:
      labels:
        app: thesis-management-backend-topic-db 
    spec:
      containers:
      - name: thesis-management-backend-topic-db
        image: qthuy2k1/thesis-management-backend-topic-db
        env:
        - name: POSTGRES_PASSWORD
          value: "root"
        imagePullPolicy: Always
        ports:
        - containerPort: 5432
        volumeMounts:
          - name: topic-db
            mountPath: /var/lib/postgresql/data
          - name: init-database-script
            mountPath: /docker-entrypoint-initdb.d/init-database-topic.sql
            subPath: init-database-topic.sql
      volumes:
        - name: topic-db
          persistentVolumeClaim:
            claimName: topic-db-pvc
        - name: init-database-script
          configMap:
            name: init-database-topic-configmap
      
      
        

---
#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-topic-db-service
spec:
  selector:
    app: thesis-management-backend-topic-db 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432