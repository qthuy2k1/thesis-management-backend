apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: submission-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: init-database-submission-configmap
data:
  init-database-submission.sql: |
    CREATE DATABASE "thesis_management_submissions" 

---
#For creating Kubernetes Deployment in the Minikube cluster
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thesis-management-backend-submission-db
  labels:
    app: thesis-management-backend-submission-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-submission-db
  template:
    metadata:
      labels:
        app: thesis-management-backend-submission-db 
    spec:
      containers:
      - name: thesis-management-backend-submission-db
        image: qthuy2k1/thesis-management-backend-submission-db
        env:
        - name: POSTGRES_PASSWORD
          value: "root"
        imagePullPolicy: Always
        ports:
        - containerPort: 5432
        volumeMounts:
          - name: submission-db
            mountPath: /var/lib/postgresql/data
          - name: init-database-script
            mountPath: /docker-entrypoint-initdb.d/init-database-submission.sql
            subPath: init-database-submission.sql
      volumes:
        - name: submission-db
          persistentVolumeClaim:
            claimName: submission-db-pvc
        - name: init-database-script
          configMap:
            name: init-database-submission-configmap
      
        

---
#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-submission-db-service
spec:
  selector:
    app: thesis-management-backend-submission-db 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432