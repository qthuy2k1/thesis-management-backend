#For creating Kubernetes Deployment in the Minikube cluster
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thesis-management-backend-comment
  labels:
    app: thesis-management-backend-comment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-comment
  template:
    metadata:
      labels:
        app: thesis-management-backend-comment 
    spec:
      containers:
      - name: thesis-management-backend-comment
        image: qthuy2k1/thesis-management-backend-comment
        imagePullPolicy: Always
        ports:
        - containerPort: 9091
      
        

---
#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-comment-service
spec:
  selector:
    app: thesis-management-backend-comment 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 9091
    targetPort: 9091