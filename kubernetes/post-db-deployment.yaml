apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: post-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: init-database-post-configmap
data:
  init-database-post.sql: |
    CREATE DATABASE "thesis_management_posts" 

---
#For creating Kubernetes StatefulSet in the Minikube cluster
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: thesis-management-backend-post-db
  labels:
    app: thesis-management-backend-post-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-post-db
  template:
    metadata:
      labels:
        app: thesis-management-backend-post-db 
    spec:
      containers:
      - name: thesis-management-backend-post-db
        image: qthuy2k1/thesis-management-backend-post-db
        env:
        - name: POSTGRES_PASSWORD
          value: "root"
        imagePullPolicy: Always
        ports:
        - containerPort: 5432
        volumeMounts:
          - name: post-db
            mountPath: /var/lib/postgresql/data
          - name: init-database-script
            mountPath: /docker-entrypoint-initdb.d/init-database-post.sql
            subPath: init-database-post.sql
      volumes:
        - name: post-db
          persistentVolumeClaim:
            claimName: post-db-pvc
        - name: init-database-script
          configMap:
            name: init-database-post-configmap
      
        

---
#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-post-db-service
spec:
  selector:
    app: thesis-management-backend-post-db 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432