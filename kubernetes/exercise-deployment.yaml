#For creating Kubernetes StatefulSet in the Minikube cluster
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: thesis-management-backend-exercise
  labels:
    app: thesis-management-backend-exercise
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-exercise
  template:
    metadata:
      labels:
        app: thesis-management-backend-exercise 
    spec:
      containers:
      - name: thesis-management-backend-exercise
        image: qthuy2k1/thesis-management-backend-exercise
        imagePullPolicy: Always
        ports:
        - containerPort: 9091
      
        

---
#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-exercise-service
spec:
  selector:
    app: thesis-management-backend-exercise 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 9091
    targetPort: 9091