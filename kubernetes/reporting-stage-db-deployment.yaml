apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reporting-stage-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: init-database-reporting-stage-configmap
data:
  init-database-reporting-stage.sql: |
    CREATE DATABASE "thesis_management_reporting_stages" 

---
#For creating Kubernetes Deployment in the Minikube cluster
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thesis-management-backend-reporting-stage-db
  labels:
    app: thesis-management-backend-reporting-stage-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thesis-management-backend-reporting-stage-db
  template:
    metadata:
      labels:
        app: thesis-management-backend-reporting-stage-db 
    spec:
      containers:
      - name: thesis-management-backend-reporting-stage-db
        image: qthuy2k1/thesis-management-backend-reporting-stage-db
        env:
        - name: POSTGRES_PASSWORD
          value: "root"
        imagePullPolicy: Always
        ports:
        - containerPort: 5432
        volumeMounts:
          - name: reporting-stage-db
            mountPath: /var/lib/postgresql/data
          - name: init-database-script
            mountPath: /docker-entrypoint-initdb.d/init-database-reporting-stage.sql
            subPath: init-database-reporting-stage.sql
      volumes:
        - name: reporting-stage-db
          persistentVolumeClaim:
            claimName: reporting-stage-db-pvc
        - name: init-database-script
          configMap:
            name: init-database-reporting-stage-configmap
      
        

---
#For creating a Kubernetes Service in the Minikube cluster
apiVersion: v1
kind: Service
metadata:
  name: thesis-management-backend-reporting-stage-db-service
spec:
  selector:
    app: thesis-management-backend-reporting-stage-db 
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432